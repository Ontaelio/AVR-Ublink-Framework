# AVR-libraries

WORK IN PROGRESS!

This library is designed for bare-metal AVR usage.
When used with Arduino core, peripheral initialization must be performed explicitly in setup(),
or Arduino core must be disabled (add main() in your sketches).